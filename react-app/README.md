# SAS Construction React Sample

This folder contains a Vite-powered React example for a 3D interactive website.

## Setup

1. Open a terminal in `react-app`
2. Run `npm install`
3. Run `npm run dev`

The app will open in your browser and show the animated 3D hero section with smooth scroll interactions.
